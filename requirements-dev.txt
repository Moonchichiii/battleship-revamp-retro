# Code Quality
black==25.1.0
ruff==0.5.6
mypy==1.17.0

# Testing
pytest==8.4.1
pytest-asyncio==1.1.0
pytest-cov==5.0.0
coverage[toml]==7.6.1

# DB Testing
pytest-postgresql==6.0.0
factory-boy==3.3.0
faker==22.0.0

# Dev Tools
pre-commit==4.0.1
pgcli==4.0.1
# Keep psycopg2-binary only if pgcli needs a system-specific lib on your machine;
# otherwise you can remove it. pgcli usually works fine without pinning here.
psycopg2-binary==2.9.9

# Performance
locust==2.37.14

# Type Stubs (pin optional; letting pre-commit install types is fine too)
types-requests
types-python-dateutil
types-redis
asyncpg-stubs
sqlalchemy-stubs
